export enum SYS_ROLE {
    SUPER_ADMIN,
    ADMIN,
    USER
}

export enum USER_AGENT {
    LOCAL,
    GOOGLE
}

export enum TOKEN_TYPE {
    ACCESS,
    REFRESH
}

export enum REACTION {
    LIKE,
    LOVE,
    HATE,
    WOW,
    SAD,
    ANGRY
}
